Para compilar use: pyinstaller --noconsole --onefile --name="GerenciadorSimuladorRecon" --icon="logo_recon-consorcio.ico" app.py

para criar o exe: usar inno setup com o arquivo inno_gerenciador.iss

para atualizar a pasta de update basta rodar o deploy.py